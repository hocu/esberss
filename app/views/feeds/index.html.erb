<h1>Listing feeds</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>User</th>
      <th>Feed updated at</th>
      <th>Url</th>
      <th>Feed</th>
      <th>Description</th>
      <th>Last build date</th>
      <th>Language</th>
      <th>Item</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @feeds.each do |feed| %>
      <tr>
        <td><%= feed.title %></td>
        <td><%= feed.user %></td>
        <td><%= feed.feed_updated_at %></td>
        <td><%= feed.url %></td>
        <td><%= feed.feed %></td>
        <td><%= feed.description %></td>
        <td><%= feed.last_build_date %></td>
        <td><%= feed.language %></td>
        <td><%= feed.item %></td>
        <td><%= link_to 'Show', feed %></td>
        <td><%= link_to 'Edit', edit_feed_path(feed) %></td>
        <td><%= link_to 'Destroy', feed, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Feed', new_feed_path %>
